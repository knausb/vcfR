# Sample .travis.yml for R projects

language: r
warnings_are_errors: true
sudo: required

r_packages:

r_github_packages:
  - jimhester/covr

after_success:
  - Rscript -e 'library(covr);coveralls()'

notifications:
  email:
    on_success: change
    on_failure: change

